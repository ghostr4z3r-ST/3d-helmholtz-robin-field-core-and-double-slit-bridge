# 11_paper_maps

Claim/section/figure maps and manuscript alignment.

The current recovered scripts are stored under `scripts/11_paper_maps/`. This `src/` directory is reserved for future normalization into importable modules.
