# 10_microscopic_robin_noise_model

Microscopic Robin-noise interpretation (currently mostly pending).

The current recovered scripts are stored under `scripts/10_microscopic_robin_noise_model/`. This `src/` directory is reserved for future normalization into importable modules.
