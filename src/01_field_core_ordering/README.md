# 01_field_core_ordering

Field-core ordering, beta scans, robustness summaries.

The current recovered scripts are stored under `scripts/01_field_core_ordering/`. This `src/` directory is reserved for future normalization into importable modules.
