# 06_double_slit_baselines

One-slit and coherent double-slit baselines.

The current recovered scripts are stored under `scripts/06_double_slit_baselines/`. This `src/` directory is reserved for future normalization into importable modules.
