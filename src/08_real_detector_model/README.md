# 08_real_detector_model

Composite detector model and screen/metric outputs.

The current recovered scripts are stored under `scripts/08_real_detector_model/`. This `src/` directory is reserved for future normalization into importable modules.
