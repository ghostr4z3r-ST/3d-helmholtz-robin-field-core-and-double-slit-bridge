# 05_field_core_function

Field-core function, transition zones, inner/outer coupling, template scans.

The current recovered scripts are stored under `scripts/05_field_core_function/`. This `src/` directory is reserved for future normalization into importable modules.
