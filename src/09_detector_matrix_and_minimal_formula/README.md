# 09_detector_matrix_and_minimal_formula

Detector matrix, parameter maps, path-marking, minimal detector law.

The current recovered scripts are stored under `scripts/09_detector_matrix_and_minimal_formula/`. This `src/` directory is reserved for future normalization into importable modules.
