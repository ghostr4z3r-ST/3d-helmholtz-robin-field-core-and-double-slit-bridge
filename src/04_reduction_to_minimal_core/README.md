# 04_reduction_to_minimal_core

Minimal kernel, carrier graph, bridge-geometry reduction.

The current recovered scripts are stored under `scripts/04_reduction_to_minimal_core/`. This `src/` directory is reserved for future normalization into importable modules.
